# このファイルは、このアプリを動かすために必要なライブラリ一覧です。
#
# バージョン指定の読み方:
#   >=3.1   → 「3.1 以上」（最低限必要なバージョン）
#   <4.0    → 「4.0 未満」（メジャーバージョンの上限）
# この書き方で「3.x 系の最新」を使いつつ、4.0 で互換性が壊れても影響を受けない。
# 上限がないと pip install 時に互換性のない新版が入って動かなくなる恐れがある。
openpyxl>=3.1,<4.0
streamlit>=1.32,<2.0
