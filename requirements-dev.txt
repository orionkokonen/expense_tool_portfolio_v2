# このファイルは、開発やテストのときだけ使う追加ライブラリ一覧です。
# requirements.txt（本番用）とは別に管理することで、
# デプロイ環境に不要な開発ツールを入れずに済む。
pytest>=8.0,<9.0
# pytest-cov = テストのカバレッジ（どの行が実行されたか）を計測する追加機能。
# 「テストが全く通っていないコード」を見つけるのに便利。
pytest-cov>=5.0,<6.0
ruff>=0.6,<1.0
mypy>=1.10,<2.0
# テスト実行時に openpyxl が必要（Excel 生成のテストがあるため）。
# 本番用 requirements.txt にも書いてあるが、開発環境でも確実に入るようここにも記載。
openpyxl>=3.1,<4.0
