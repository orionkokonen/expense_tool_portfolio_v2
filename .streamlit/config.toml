# このファイルは、Streamlit画面の動き方を決める設定です。
[server]
# このプロジェクトでは画面テーマよりも「どこでも起動できること」を優先し、
# まず必要最小限の設定だけをここに置いている。
# headless = true: ブラウザを自動で開かない設定。
# Render などのサーバー環境ではブラウザがないため、true にしないと起動エラーになる。
headless = true
