<!-- このファイルは、これから作りたい機能や改善案をまとめる計画書です。 -->

# ロードマップ

今後の実装予定や改善アイデアを管理するためのファイル。実装の難易度順（やさしい → むずかしい）に並べています。

## 使い方

- 完了した項目は `CHANGELOG.md` に移す
- 大きい変更は目的や影響範囲も一緒に残す




## 1段階目: 既存コードの軽い拡張で済むもの（最も簡単）

1. **ダッシュボード表示**: 既存の集計結果（月別・カテゴリ別）を Streamlit のグラフ部品（`st.bar_chart` など）で見せるだけ。新しいロジックはほぼ不要。
2. **上限金額チェック**: `rules.json` にカテゴリ別の上限を追加し、既存ルールチェックに条件を1つ足すだけ。
3. **重複申請チェック（既存の拡張版）**: 既に重複検出ロジックがあるので、照合キー（利用日 + 金額 + 申請者 + 支払先）を増やす拡張。
4. **匿名化出力**: 出力前に氏名・会社名の列を `***` 等に置換する処理を1つ挟むだけ。
5. **複数CSVの一括処理**: 既存パイプラインを `for` ループで複数ファイルに回し、結果を `pd.concat` で結合するだけ。
6. **サンプルCSVの中身をクリックだけで見られるようにする**: `st.expander` 内で `pd.read_csv(サンプルファイル)` を `st.dataframe` で表示するだけ。

## 2段階目: 新しい小機能を追加するレベル

7. **監査ログ**: 実行イベントをログファイルに追記する処理を追加。Python標準の `logging` でOK。
8. **加盟店名の正規化辞書**: JSON辞書（`{"AMAZON": "Amazon"}`）を作り、文字列を置換する関数を1つ追加。
9. **申請一覧の絞り込み・検索**: Streamlit のセレクトボックス + DataFrame の `.query()` でフィルタ。
10. **会計ソフト向けエクスポート**: freee / マネフォ / 弥生の仕様に合わせて列名・順序を変えるCSV出力関数を追加。
11. **インポートプリセット保存**: UI設定をJSONで保存 / 読込するだけ（`json.dump` / `json.load`）。
12. **CSV以外の形式（Excel）でもアップロードできるようにする**: 拡張子で分岐し `pd.read_excel()` を呼ぶ。シート選択UIを追加。

## 3段階目: それなりにロジックや UI が必要

13. **CSV列マッピング機能**: Streamlit でドロップダウン UI を作り、列名 → 標準名の辞書を作って `df.rename()`。
14. **申請ルール違反チェック**: 既存の警告ロジックを「申請単位」に拡張。判定条件が増えるので分岐管理が必要。
15. **領収書添付の有無チェック**: `st.file_uploader` で添付管理 + ルール判定。ファイル保存先の設計が必要。
16. **走行距離・日当計算**: 距離・日数からの計算式と入力フォームを追加。
17. **ルールシミュレーター**: 既存のチェック処理を「仮ルール」で再実行し件数を比較。
18. **税額・仕訳の自動補助**: カテゴリ → 勘定科目のマッピング表 + 税率テーブルを作り、候補を提示。
19. **多通貨対応**: 通貨列 + レート表を追加し、円換算ロジックを通す。
20. **実行履歴の比較**: 実行結果のサマリを保存し、前回との差分を表示。
21. **ルールのバージョン管理**: `rules.json` の更新履歴を別ファイルに残し、どの版で判定したかを記録。
22. **エラー行のその場修正UI**: `st.data_editor` で編集 → 再チェック。状態管理（`st.session_state`）が必要。

## 4段階目: 永続化・外部連携・データモデル変更が必要（難しい）

23. **経費申請の CRUD**: DB（SQLite等）導入が事実上必要。今のファイル処理アプリから「アプリケーション」への進化。
24. **API / Webhook連携**: 外部HTTP通信、認証情報管理、エラー処理（リトライ等）が必要。
25. **異常検知**: 過去データの統計処理。標準偏差ベースなら中、機械学習なら難。
26. **コストセンター・PJ按分**: 1件 → 複数明細のデータモデル変更。集計ロジック全般に影響。
27. **レシートOCR取り込み**: Google Vision API 等の外部OCRサービス連携 + 画像処理 + 抽出結果の整形。

## 5段階目: アプリ基盤の作り直しが必要（最も難しい）

依存関係上、28 → 29 → 30 の順で実装する必要があります。

28. **承認ワークフローの基本版**: ユーザー管理 + 申請ステータス（下書き / 申請中 / 承認 / 差戻し）+ 通知。DB + 認証が必須。
29. **差戻しコメント機能**: 差戻し時に理由コメントを必須にし、申請者が修正・再申請できるようにする。承認ワークフロー実装後の追加機能。
30. **権限管理 / SSO**: 認証基盤の構築。OAuth連携・ロール管理・セッション管理など、別アプリ並みの工数。



