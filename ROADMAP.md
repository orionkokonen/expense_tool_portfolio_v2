<!-- 学習用メモ: ROADMAP は将来の予定表。今の実装内容とは分けて読む。 -->

# ロードマップ

今後の実装予定や改善アイデアを管理するためのファイルです。

## 使い方

- `現在`: 直近で着手する項目
- `次`: 次のリリース候補
- `今後`: 中長期で検討する項目
- 完了した項目は `CHANGELOG.md` に移す
- 大きい変更は目的や影響範囲も一緒に残す

## 現在

- CSV 以外の形式でもアップロードできるようにする
- サンプル CSV の中身をクリックだけで見られるようにする

## 次

## 今後

### 将来実装したい機能

- `経費申請の CRUD`: 申請の作成・一覧・詳細確認・編集・削除を一通り行えるようにする。
- `レシートOCR取り込み`: レシート画像から日付・金額・支払先などを抽出し、申請フォームへ反映する。
- `申請ルール違反チェック`: 金額上限や領収書必須などのルール違反を検知し、警告または送信制御を行う。
- `承認ワークフローの基本版`: 申請者から承認者への基本的な承認フローとステータス管理を実装する。
- `税額・仕訳の自動補助`: 税額計算と勘定科目の候補提案を自動化し、入力負荷を下げる。
- `差戻しコメント機能`: 差戻し時に理由コメントを必須にし、申請者が内容を確認して修正・再申請できるようにする。
- `申請一覧の絞り込み・検索`: 月別・部門別・ステータス別・申請者別などで一覧を絞り込み、目的の申請を探しやすくする。
- `上限金額チェック`: カテゴリごとの上限金額や事前申請必須などの条件を設定し、申請時に自動チェックする。
- `ダッシュボード表示`: 今月の申請件数、未承認件数、差戻し件数、カテゴリ別合計金額などをトップ画面で見える化する。
- `重複申請チェック（既存の拡張版）`: 既存申請と利用日・金額・申請者・支払先を照合し、重複の可能性がある申請を警告表示する。
- `領収書添付の有無チェック`: 領収書の添付有無を管理し、一定金額以上では添付必須にするなどのルールを適用できるようにする。

## アイデア / バックログ

追加候補の一覧。

| 項目                       | 初心者向け説明                                                                                 |
| -------------------------- | ---------------------------------------------------------------------------------------------- |
| CSV列マッピング機能        | CSVごとに違う列名を、「これは日付」「これは金額」と画面で対応付けできる機能。                  |
| 監査ログ                   | 誰がいつ何をしたかを記録する機能。実行、修正、出力、ルール変更の履歴を残せる。                 |
| API / Webhook連携          | 他のシステムと自動でつなぐ機能。CSV送信やSlack通知などができるようになる。                     |
| 加盟店名の正規化辞書       | `Amazon`、`AMAZON`、`Amazon Japan` のような表記ゆれを同じ会社としてまとめる機能。              |
| 会計ソフト向けエクスポート | freee、マネフォ、弥生などにそのまま取り込める形式でデータを出力する機能。                      |
| エラー行のその場修正UI     | エラーが出た行を画面上で直接直し、そのまま再チェックできる機能。                               |
| 匿名化出力                 | 名前や会社名などを隠してレポートを出す機能。デモや共有に向いている。                           |
| インポートプリセット保存   | 一度作った取り込み設定を保存し、「カード会社A用」などとして次回すぐ使える機能。                |
| ルールシミュレーター       | ルールを変更したら警告件数がどう変わるか、実際に適用する前に試せる機能。                       |
| 複数CSVの一括処理          | 複数のCSVをまとめて読み込み、合算した結果や統合レポートを出す機能。                            |
| 多通貨対応                 | 円だけでなくドルやユーロなども扱い、決めたレートで円換算できる機能。                           |
| 権限管理 / SSO             | 人によってできる操作を分ける機能。SSOはGoogleやMicrosoftなどのアカウントでログインする仕組み。 |
| 実行履歴の比較             | 今回と前回の結果を比べて、「エラーが減った」「警告が増えた」などを見られる機能。               |
| ルールのバージョン管理     | ルールをいつ誰が変えたかを記録し、どのルールで判定したか後から追える機能。                     |
| コストセンター・PJ按分     | 1件の経費を複数の部署やプロジェクトに分けて計上できる機能。                                    |
| 異常検知                   | 過去データと比べて、不自然に高い金額や怪しい使い方を見つけて警告する機能。                     |
| 走行距離・日当計算         | 出張や移動に関する経費を、距離や日数から自動計算する機能。                                     |
