# このプロジェクトについて

Streamlit製の経費精算ツールのポートフォリオ。

主な要素:

入口: CLI (expense_tool.py) と GUI (app.py Streamlit) の2系統で同じパイプラインを共有
機能: CSV取込 → 入力チェック / 重複検出 / 社内ルール違反検出 → 月別・カテゴリ別・Top Nなどの集計
出力: CSV(errors/warnings/clean/summary) + Excel (excel_export.py) + HTML (html_report.py)
ルール: rules.json で未登録カテゴリ・禁止ワード・上限額などを外部定義
セキュリティ: 数式インジェクション対策、パストラバーサル対策、HTML/JSONのXSSエスケープ、run_idで実行ごとに成果物を分離
品質管理: pytest / ruff / mypy / GitHub Actions
デプロイ: Render (render.yaml)、DB・認証なしのファイル処理アプリ


# ユーザーについて

- プログラミングの勉強はpythonエンジニア認定基礎試験合格ぐらいで、それ以外は全く経験のない初心  者。プログラミングやITに関するほとんどの知識が無いことを前提として、初心者向けに噛み砕いて、専門用語の意味も補足して解説をしてください
- コードを解説するときは、必ず以下の2点を含めること：
  （1）各行が何をしているかを1行ずつ表形式で説明する（「コード」「1行の意味」の2列）。
  （2）コード内で使われている関数・メソッド・属性・変数などの「部品」について、それぞれの役割を箇条書きで解説する。短いコードでも省略せず、初心者がコピペで理解できる粒度で書くこと。
- コード例で複数のファイルにまたがる内容を説明する場合、必ず(ファイル名.py — 役割の一言)のようにファイル名と役割を各コードブロックの先頭に書いて、どのファイルのコードかを明示すること。1つのコードブロックに複数ファイルの内容を混ぜない。
- たとえ話で説明するときは、たとえに出てくる各要素が元の話のどの概念に対応するかを必ず示すこと。例：「入場パス（＝Cookie）」「整理券（＝CSRFトークン）」のように、要素ごとに対応関係を併記する。
 - 複数ステップ（例：5つのステップ）の解説をする際、現在どのステップにいるかを順次明示してほしい。例：「今は3つめのステップに該当する」と示す形式でおねがい。
- 説明の際には、その分野のプロとして初心者でも理解できるように、結論➡理由➡具体例の順で、そのまま使える形で説明することを希望。
- 「覚え方のコツ」があると、理解しやすい。
